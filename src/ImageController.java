/**
 * An interface to represent controllers for different types of images which can perform various
 * processing operations.
 */
public interface ImageController {
  /**
   * Executes the controller, allowing input to control the image processing functionality.
   */
  void execute();
}